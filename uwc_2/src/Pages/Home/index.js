import { useNavigate } from 'react-router-dom';
import { SideBar } from '~/components';
function Home() {
    return (
        <div>
            <h1>Home</h1>
            <SideBar />
        </div>
    );
}

export default Home;
